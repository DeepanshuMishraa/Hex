---
"hex-app": patch
---

Fix AI post-processing with correct Groq API format and enhanced features

Fixed the AI post-processing feature with:
- **Fixed Groq API request format**: Changed from incorrect `maxTokens` to correct `max_tokens` field name
- Using `moonshotai/kimi-k2-instruct` model
- Improved system prompts that better handle self-corrections (e.g., "at 3pm oh sorry at 4pm" → "at 4pm")
- Added API key validation with a "Validate" button in Settings
- Real-time validation feedback showing success/error states
- More comprehensive filler word removal
- Better preservation of speaker intent and tone across all modes (Auto, Email, Messages, Commands)
- Added debug logging for API requests to help troubleshooting
